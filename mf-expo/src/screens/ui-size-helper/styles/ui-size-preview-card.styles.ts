import { StyleSheet } from 'react-native';

export const uiSizePreviewCardStyles = StyleSheet.create({
  container: {
    borderRadius: 16,
    padding: 20,
    borderWidth: 1,
    marginBottom: 20,
  },
  title: {
    fontSize: 18,
    marginBottom: 16,
  },
  infoRow: {
    marginBottom: 16,
  },
  infoText: {
    fontSize: 14,
  },
  previewSection: {
    marginBottom: 20,
  },
  sectionTitle: {
    fontSize: 16,
    marginBottom: 12,
  },
  spacingRow: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
  },
  valueText: {
    fontSize: 14,
  },
});

