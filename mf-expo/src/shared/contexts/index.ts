/** useLocale: import from `@/src/shared/hooks/use-locale` (avoids duplicate export via shared barrel). */
export { LocaleProvider } from './locale-context';
export { ThemeProvider, useTheme } from './theme-context';

