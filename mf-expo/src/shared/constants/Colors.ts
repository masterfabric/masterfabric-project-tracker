/**
 * App theme colors — re-exported from masterfabric-expo-core (single source of truth).
 * Light mode is white-first with a neutral charcoal accent (not system blue).
 */
export {
  accentDark,
  accentLight,
  Colors,
  getThemeColors,
  QUICK_ACTION_COLORS,
} from 'masterfabric-expo-core';
